# Example Report

## Scenario

A staged export passes manifest verification, but the session database schema differs. The import remains BLOCKED for session history while skills and non-secret configuration can proceed through a reviewed partial plan.

## Expected behavior

- Follow the required procedure.
- Cite or name the evidence behind each material finding.
- Select one allowed classification.
- Record unavailable evidence.
- End with approval-gated next actions rather than silently performing them.
