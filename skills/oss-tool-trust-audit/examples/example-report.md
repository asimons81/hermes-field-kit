# Example Report

## Scenario

The source is legitimate and telemetry-free, but the package executes unrestricted shell commands and releases are built manually without provenance. The verdict is USE WITH CONTROLS in an isolated environment.

## Expected behavior

- Follow the required procedure.
- Cite or name the evidence behind each material finding.
- Select one allowed classification.
- Record unavailable evidence.
- End with approval-gated next actions rather than silently performing them.
